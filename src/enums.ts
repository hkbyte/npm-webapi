export enum RequestMethod {
    GET = 'GET',
    POST = 'POST',
    DELETE = 'DELETE',
    PATCH = 'PATCH',
    HEAD = 'HEAD',
    PUT = 'PUT',
}

export enum ResponseType {
    JSON = 'JSON',
    HTML = 'HTML',
}

export enum ResponseStatus {
    OK = 200,
    CREATED = 201,
    NO_RESPONSE = 204,
}
